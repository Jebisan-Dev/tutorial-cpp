#include <bits/stdc++.h>

using namespace std;

int main()
{
    char alpha;

    cin >> alpha;

    if (alpha == 'a' || alpha == 'b' || alpha == 'c' || alpha == 'd' || alpha == 'e')
    {
        cout << "vowel" << endl;
    }
    else
    {
        cout << "consonant" << endl;
    }
}